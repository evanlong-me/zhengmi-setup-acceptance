# zhengmi setup acceptance

Three-OS acceptance matrix for the one-click setup scripts served by the
zhengmi gateway. Every job downloads the script from the production gateway
(or the CDN mirror), runs it, and verifies both the written config and the
installed tool. Nothing here is a source of truth for the scripts.

Run it from the local skill workflow; the public repo exists so Actions
minutes are unmetered.
